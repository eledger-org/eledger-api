{
  "rules": {
    "no-unused-vars": [
      "error",
      {
        "vars": "all",
        "args": "after-used",
        "varsIgnorePattern": "(migrates)|(finalCreates)|(Log)",
        "argsIgnorePattern": "(resolve)|(reject)"
      }
    ]
  }
}
